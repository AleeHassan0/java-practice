import java.util.*;
public class input {
    public static void main(String[] args) {
        Scanner sc =new Scanner(System.in);//new we have taken input but still not store
   String name=sc.nextLine(); //now we have store the input into name
   System.out.println(name); //just next world will print first world of the line only..if you want to print whole line then you have to write nextline.....
    }
}
